# 党建大屏改造说明（固定域名 + 数据脱敏）

本次改造解决两个问题：**① 大屏改用 GitHub Pages 固定域名取数**；**② 补全会议自由文本的脱敏，杜绝姓名/手机号/身份证/邮箱泄露**。

---

## 一、固定域名与各页面地址

| 用途 | 地址 |
|---|---|
| 大屏展示页（电视全屏） | `https://dawndsre.github.io/party-screen/` |
| 大屏展示页（等价） | `https://dawndsre.github.io/party-screen/index.html` |
| 后台管理页 | `https://dawndsre.github.io/party-screen/admin.html` |
| 大屏数据文件（JSON） | `https://dawndsre.github.io/party-screen/screen-data.json` |

- 大屏页使用 **相对路径** `./screen-data.json` 取数，因此部署到上述固定域名后**无需改任何代码**即自动从该域名拉取。
- 支持用 URL 查询参数临时覆盖数据源（便于验证 / 切换 / 回退）：
  - `index.html?src=./screen-data.json`（默认）
  - 例：`https://dawndsre.github.io/party-screen/?src=https://其他地址/screen-data.json`
- 页脚实时显示「**数据源：<实际 URL>**」与「**最后更新：<时间>**」，便于确认数据来源与新鲜度。

### 生成固定域名的前置条件（由主理人操作）
1. GitHub 仓库 `DawnDsre/party-screen` 的 **Settings → Pages → Source = GitHub Actions**；
2. `main` 分支推送后，`.github/workflows/pages.yml` 自动构建并发布；
3. 发布成功后该固定域名即生效（此前为 404，属正常）。

---

## 二、脱敏规则清单

发布（后台「保存并发布」）时，由 `admin.html` 的 `buildScreenData()` 统一生成 `screen-data.json`，规则如下：

### 1. 党员名册 —— 聚合与泛化
| 字段 | 处理方式 | 结果 |
|---|---|---|
| 姓名 | **完全剔除**，不进入展示数据 | 个体消失 |
| **出生年份 → 年龄段**（规则①） | 源数据**不再保存精确出生年份**，仅保存 5 档**年龄段** `age_group`；`stats.age` 直接按年龄段计数 | `≤30 / 31-40 / 41-50 / 51-60 / >60` |
| `position`（党内职务） | **泛化为层级**：`(position && position!=='党员') ? '支委' : '党员'` | 只保留「支委/党员」 |
| 性别 / 学历 / 身份 | 聚合为**统计计数**（`stats`） | 只见分布，不见个体 |
| 部门 / 荣誉 | 保留（不含个人身份信息） | 组织维度 |

> `stats`（`total / formal / probation / gender / education / age / status`）为纯计数，不还原到个体。
> **准标识符防护**：移除精确出生年份，可避免「出生年 + 职务 + 部门 + 学历 + 企业全称」组合被用于重新识别到具体个人。

### 2. 会议文本 —— `desensitizeText()` 统一脱敏
对 `title / location / content / summary` 四个自由文本字段依次执行：

| 顺序 | 规则 | 示例 |
|---|---|---|
| ① | **姓名 + 「同志」称谓** → 脱敏（见下方策略） | `欧阳修同志` → `该同志`；`讨论并通过刘洋同志…` → `讨论并通过该同志…` |
| ② | **身份证 18 位** → 前 6 + `********` + 后 4 | `530102199001011234` → `530102********1234` |
| ③ | **手机号 11 位** → 前 3 + `****` + 后 4 | `13812345678` → `138****5678` |
| ④ | **邮箱** → 首字符 + `***@域名` | `zhangsan@abc.com` → `z***@abc.com` |
| ⑤ | **相邻脱敏片段合并**（仅可读性） | `该、该和该同志` → `该同志` |

> 说明：上表及本文件中出现的姓名、身份证号、手机号、邮箱均为**为演示脱敏效果而编写的虚构样例**，与任何真实人员信息无关。

**① 姓名脱敏采用「同志锚定 + 向左扫描」策略**（不再用单个正则直接匹配，见下方「为什么不用正则」）：

1. **锚点定位**：先用 `lastIndexOf('同志')` 定位每一个「同志」，再向左扫描收集姓名串——允许字符为汉字 / `·` / 顿号 / 空格 / 拉丁字母（支持中英混排），**遇其它标点即止**；扫描会**整段跨过括号补充**（`（预备党员）` / `【…】` / `《…》` / `「…」` / `『…』` 等成对括号），因此 `张三（预备党员）、李四（女）同志` 中的两个姓名都会落入同一串被处理；从右向左逐个处理，右侧已处理部分不受左侧替换影响。
2. **剥离泛称**：从右向左剥离集体泛称与个人职务头衔——若整段皆为泛称则**原样保留**。
3. **切分连写**：按「切分符」把姓名串分段，**切分符本身原样保留**，因此可还原出 `该（预备党员）、该（女）` 这类可读结果。切分符含：连接词 `、和与及`、顿号逗号 `、，,`、省略号、各类成对括号组。省略号写法按实际录入习惯覆盖四种：`…`（U+2026）、`⋯`（U+22EF）单字符即成省略号；ASCII 点 `...` 与中文句号 `。。。` 需**连续 ≥2 个**才算省略号（单个 `.` / `。` 是正常断句，若误判会吞掉上一句的姓名）。
4. **定界**：对每一段调用 `nameStartIndex()`，取**「最长且首字为已知姓氏」的 2–4 字后缀**作为姓名段起点：
   - **最长优先**是关键：`华罗庚` 若从 2 字下限切入会被误判为「罗庚」，把真正的姓氏「华」当语境留在明文里（旧版即产生 `华该同志` 这类单字残留）；
   - 起点 ≤ 0（姓名即整段 / 无法定位）→ 该段**整体**脱敏；
   - 起点左侧**仅剩 1 个汉字** → 判定为姓名首字，一并脱敏；
   - 起点左侧 **≥ 2 字** → 判定为语境，保留。
   - 姓氏表 `SINGLE_SURNAMES` 已由百家姓前 100 扩充约 120 个常见姓氏，`COMPOUND_SURNAMES` 覆盖 `欧阳/司马/上官/诸葛/长孙/慕容/皇甫/令狐…` 约 50 个复姓。
5. **逗号/省略号分句 vs 名单**：`，` 与 `…` 不能无条件跨越。
   - `，`：仅当「逗号左侧那一段本身就像一个完整姓名（剥离泛称后 2–4 汉字且能定位到姓氏）」时才继续向左延伸。于是 `张三，李四，王五同志` → `该同志`，而 `他强调了纪律，张三同志` → `他强调了纪律，该同志`。
   - `…`：仅当省略号左侧「≤4 字的姓名原子」成立时才跨越（左侧可先跳过括号补充）；否则不消费省略号，避免产生悬空的「……该」。省略号字符集的判定与上文字符集一致（`…`/`⋯`/`...`/`。。。` 等价处理）。于是 `张三……李四同志` → `该……该同志`、`张三...李四同志` → `该...该同志`、`名单：张三（预备党员）……李四同志` → `名单：该（预备党员）……该同志`。单个句号不跨越：`张三。李四同志记录。` → `张三。该同志记录。`（上一句姓名保留，不误吞正文）。
   - 跨越 `、/，` 时逐段校验左侧片段：`张三、李四、…（23 人）…许二三同志` → 整串脱敏；而 `研发部、财务部、张三同志` → `研发部、财务部、该同志`（部门不被当成姓名）。
6. **组织词保护**：定位不到姓氏且以机构尾缀收尾的片段（`部/科/室/组/中心/公司/集团/厂/处/局/院/所/委/会/班/系/队/站/办/事业部/党支部/党总支/党委/车间/分公司`）判定为组织名，**原名保留**，避免部门名被整串脱敏。
7. **合并相邻脱敏片段**：`该、该和该` → `该`，`该同志与该同志` → `该同志`（纯可读性，不涉新增泄露）。

> **为什么不用单个正则**：早期实现用 `([汉字·](?:(?!同志)[汉字、·\s]){0,39})同志`，存在三处结构性漏判——
> a) 字符类不含括号/逗号 → `张三（预备党员）同志`、`张三，同志` 整段不匹配，**直接明文输出**；
> b) 字符类不含拉丁字母 → 中英混排 `张San同志` 不匹配；
> c) `{0,39}` 长度上限 → 超过 40 字的名单只有末尾一段被脱敏，**前段姓名明文残留**。
> 这三类均已由「锚点 + 向左扫描」方案消除，并有对应用例回归。

- **通用/职务称谓保护**：`全体党员同志` / `各位同志` / `广大同志` / `支部书记同志` / `向组织同志` / `钟南山院士同志`（→`该院士同志`）等**不会被破坏**。
- **语境保留**：`会议由支部书记张伟同志主持` → `会议由支部书记该同志主持`；`研发部王小明同志` → `研发部该同志参会`；`老党员李四同志` → `老党员该同志`。
- **过度脱敏防护**：`他在报告中指出，同志们都应学习` / `会上强调，同志们要发挥作用` **保持原样**。
- **覆盖范围**：复姓、少数民族姓名（`阿依古丽·买买提`、`买买提·艾力`）、多姓名连写（`刘洋和赵刚` → `该同志`）、单字称谓（`李同志` → `该同志`）、含空格（`张 三同志` → `该同志`）、长名（`钟南山院士` → `该院士同志`）、括号补充（`张三（预备党员）同志` → `该（预备党员）同志`；`刘洋(预备党员)同志` → `该(预备党员)同志`；`张三【预备党员】同志` → `该【预备党员】同志`）、中缀标点（`张三，同志` → `该，同志`）、中英混排（`张San同志` → `该同志`）、超长名单（23 人名单 → `该同志`）。
- **幂等**：对已脱敏文本二次运行结果不变（`讨论并通过该同志…` 保持原样）。
- **`host`（主持）角色白名单**（规则②）：仅当主持人**精确等于**下列角色时原样保留，其余（含 `张伟书记`、`党支部副书记张三` 等「姓名+职务」）一律泛化为 `负责人`：
  `党支部书记 / 党支部副书记 / 书记 / 副书记 / 组织委员 / 宣传委员 / 纪检委员 / 委员 / 党小组长 / 支部委员`。
- `mtype`（会议类型）为固定枚举，无需脱敏。

### 3. 兜底校验
`screen-data.json` 及 `data/party.json` 中**不含任何真实姓名、手机号、身份证号、邮箱**。

---

## 三、更新触发机制（数据流）

```
① 后台发布          ② 仓库提交            ③ Pages 部署          ④ 大屏轮询
admin.html    →   data/party.json     →   .github/workflows/  →  index.html
「保存并发布」      screen-data.json        pages.yml 自动构建     每 30 秒 fetch
（自动脱敏）       （自动生成·已脱敏）     并发布到固定域名       ./screen-data.json
```

1. 在 `admin.html` 登录 GitHub 仓库，编辑支部信息 / 三会一课 / 党员名册；
2. 点「**保存并发布**」→ 自动写入 `data/party.json`（源数据）并生成脱敏后的 `screen-data.json`；
3. 仓库收到提交 → `pages.yml` 工作流自动构建并发布到固定域名；
4. 大屏页 `setInterval(loadData, 30000)` **每 30 秒**以 `{cache:'no-store'}` 重新拉取，自动刷新；
5. **拉取失败时保留上一次数据**（不会白屏），页脚「最后更新」维持最近一次成功时间。

> 端到端延迟 ≈ 后台发布 → 提交 → Pages 构建（约 1–2 分钟）→ 大屏下一轮轮询（≤ 30 秒）。

---

## 四、公开发布面收敛（重要）

**问题背景**：GitHub Pages 默认若以整仓根目录（`path: '.'`）发布，则站点会**公开伺服仓库内所有文件**——包括 `data/party.json`（含 12 条 `members[]` 的职务/部门/年龄段/学历等**准标识符**）。任何人访问 `https://dawndsre.github.io/party-screen/data/party.json` 即可下载完整源数据表，这是脱敏要防的再识别风险。

**修复**：`.github/workflows/pages.yml` 改为**仅发布白名单目录 `_site`**：

1. 在 `upload-pages-artifact` 之前新增步骤 `Build publish whitelist (_site)`（`shell: bash`），把大屏运行真正需要的 3 个文件复制进 `_site`：
   ```bash
   rm -rf _site && mkdir -p _site
   cp -f index.html screen-data.json admin.html _site/
   ```
   （缺任一文件即报错退出，不静默跳过。）
2. `upload-pages-artifact` 的 `path` 由 `.` 改为 `_site`。

**站点实际发布内容（白名单）**：

| 发布路径 | 说明 |
|---|---|
| `/index.html` | 大屏展示页（`fetch('./screen-data.json')` 在 `_site` 根下成立） |
| `/screen-data.json` | 聚合后的脱敏数据 |
| `/admin.html` | 后台管理页 |

**不再通过站点伺服**：`data/party.json`、`README.md`、`改造说明.md`、`deploy.*`、`*.bat`、`_*.js`、`.github/` 等。

**残余风险（如实标注）**：本方案只收敛了 **Pages 站点**的发布面。仓库本身仍为 **public**，因此 `data/party.json` 仍可通过 **GitHub 网页 / raw / API** 直接读取。若要彻底消除该暴露，须将**源数据 `data/party.json` 迁移到私有仓库**（`admin.html` 的仓库 owner/repo 字段可直接切换指向该私有仓库），而 `screen-data.json` 继续发布到本公开仓库——但注意：**当前 GitHub 免费计划下私有仓库不支持 Pages**，故 Pages 站点必须留在公开仓库。若必须在免费计划下彻底隐藏源数据，可考虑改用外部后端托管源数据。

---

## 五、一键启动脚本

| 脚本 | 作用 |
|---|---|
| `启动大屏（Edge全屏）.bat` | 用 Edge `--kiosk` 全屏模式打开展示页（电视端双击即可） |
| `启动后台.bat` | 打开后台管理页 |

> 脚本以 `chcp 65001` 兼容 UTF-8 中文显示；若电视端未装 Edge，请改用本机默认浏览器手动打开固定域名。
